<div class="hero-unit">
  <form id="nominatedBadge-form" class="form-horizontal" action="/nominated_badges" method="POST">
    <fieldset>
      <legend>Create a new NominatedBadge</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {nominatedBadge: {}}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
